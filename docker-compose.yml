services:
  api:
    build:
      context: .
      dockerfile: docker/hub.Dockerfile
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:///data/alpha.db
    volumes:
      - ./forge/data:/app/data

